public class Enums
{
    enum direction{whiteSide,blackSide,allSide};
    enum wayOfMove{row, colm,diagonal,knightWay};
    enum colm {A,B,C,D,E,F,G};
    enum kindOfTool {king, queen,knight,rook,pawn,bishop};
    enum toolColor {white, black};
}
